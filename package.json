{
  "name": "taskflow",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "turbo db:push --filter=@taskflow/api",
    "db:seed": "turbo db:seed --filter=@taskflow/api",
    "db:generate": "turbo db:generate --filter=@taskflow/api",
    "db:migrate": "turbo db:migrate --filter=@taskflow/api"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
